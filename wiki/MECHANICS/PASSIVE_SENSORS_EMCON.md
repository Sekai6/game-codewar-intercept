# 被动传感器与 EMCON

IRST 依据发动机热特征和视角生成方位为主的航迹；ESM 只在目标雷达、通信或干扰机主动辐射时建立辐射源航迹。两者均不读取目标真值坐标。

平台模式：

- `ACTIVE`：允许主动雷达与被动传感器。
- `EMCON`：雷达和主动通信尽量静默，IRST/ESM 与数据链接收继续工作。
- `PASSIVE_ONLY`：仅允许 IRST、ESM、本机旧航迹和数据链 cue。

被动航迹通过 Link 11/Link 16 共享后仍是 cue，不能直接授权武器；必须由接收平台本机传感器确认。EMCON 切换、被动建轨、融合和过期均进入 AAR。
